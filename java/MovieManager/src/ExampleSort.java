import java.util.Arrays;
import java.util.Collections;
import java.util.List;

public class ExampleSort {
    List<Integer> numbers = Arrays.asList(5,2,3,4,1,9);

    // Sort in ascending order
    Collections.sort(numbers);
}
